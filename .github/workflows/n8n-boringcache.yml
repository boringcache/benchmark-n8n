name: "n8n - BoringCache"
run-name: >-
  BC | ${{ github.event_name == 'push' && github.event.head_commit.message || github.event_name == 'pull_request' && format('PR #{0}', github.event.pull_request.number) || format('Manual dispatch ({0})', github.ref_name) }}

on:
  workflow_dispatch:
    inputs:
      cli_ref:
        description: "Optional boringcache/cli ref to build and use instead of the released CLI"
        required: false
        type: string
  push:
    branches:
      - main
    paths:
      - ".gitmodules"
      - "upstream"
  pull_request:
    paths:
      - ".gitmodules"
      - ".github/workflows/n8n-boringcache.yml"
      - ".github/workflows/n8n-actions-cache.yml"
      - ".github/workflows/reusable-node-turbo-benchmark.yml"
      - "README.md"
      - "scenarios/**"
      - "scripts/**"
      - "upstream"

permissions:
  contents: read
  actions: read

jobs:
  benchmark:
    uses: ./.github/workflows/reusable-node-turbo-benchmark.yml
    with:
      benchmark_id: n8n
      project_repo: n8n-io/n8n
      strategy: boringcache
      node_version: "24.13.1"
      install_command: CI=1 pnpm install --frozen-lockfile
      build_command: CI=1 pnpm turbo run build --summarize
    secrets: inherit
